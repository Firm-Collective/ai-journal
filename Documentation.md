## File Structure

**Folders**:
- `app`
  - within the app folder, will contain all our routes
  - we have a `(auth)` : these are groups
  - we have a `(user)` : these are groups
- `assets`
  - file in here does something else
- `components`
- `constants`
- `lib`
  - any extra helper functions or utilities or hooks that we use within the project
- `models`
- `providers`

## Code Styles
How do we structure the components?

1. Imports and other `.ts` functgions
2. Main .tsx component
3. stylesheet

Possibly give a picture or example of this...

## Explain the harder concepts

### Authentication
- how does the authentication work?
- auth provider
- supabase auth functions

#### Login flow 

#### Signin with Google/Facebook/Apple Branch

#### User sanitation
- how does the user sanitation work?
- at the moment all client handled

### Offline Support
- explain the watermelon db folder and structure and functions
- journal Entries provider
- network provider


**TODO: **
